import RouterConfig from '@/infrastructure/navigation';
import GlobalStyle from '@/infrastructure/theme';

const App = () => {
	return (
		<>
			<RouterConfig />
			<GlobalStyle />
		</>
	);
};

export default App;
