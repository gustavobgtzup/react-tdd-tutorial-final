import { Margins, Paddings } from '@/infrastructure/theme';
import { DefaultTheme } from 'styled-components';

export type GetStylesCommon = {
	margins: Margins;
	paddings: Paddings;
	theme: DefaultTheme;
};
