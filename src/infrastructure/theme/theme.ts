import { breakpoints } from './breakpoints';
import { fonts, fontWeights, fontSizes, lineHeights } from './fonts';
import { light } from './pallete';
import { space } from './spacing';

export const theme = {
	pallete: light,
	fonts,
	fontSizes,
	fontWeights,
	lineHeights,
	space,
	breakpoints,
};
